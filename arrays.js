var chocolateBars= ["snickers", "hundred grand", "kitkat", "skittles"];
//define two functions, Both functions take two parameters, an array and an element to add to the beginning of the array,
//and both functions should add the element to the beginning of the array and then return the whole array
