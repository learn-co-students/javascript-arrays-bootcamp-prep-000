{
var chocolateBars = ["snickers", "hundred grand",
  "kitkat", "skittles"]

function addElementToBeginningOfArray("e",...chocolateBars){}

function destructivelyAddElementToBeginningOfArray(
  chocolateBars.unshift("x")){}

  function addElementToEndOfArray(...chocolateBars, "e"){}

  function destructivelyAddElementToEndOfArray(
    chocolateBars.shift("x")){}


function accessElementInArray(chocolateBars, chocolateBars(1)){
  console.log(chocolateBars(1))
}

const days = ["Monday", "Tuesday", "Wednesday"]

function destructivelyRemoveElementFromBeginningOfArray(days) {
  days.shift;
  return days
}

var cats = ["Milo", "Garfield", "Otis"]

function removeElementFromBeginningOfArray(cats){
  cats.slice(1)
}

function destructivelyRemoveElementFromEndOfArray(cats) {
  cats.pop()
}

var iceCreams = ["chocolate", "vanilla", "raspberry"]

function removeElementFromEndOfArray(cats) {
  cats.slice(0, cats.length - 1)
}
.done();
}
